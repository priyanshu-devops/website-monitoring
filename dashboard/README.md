# Dashboard UI

This folder contains a React + Vite dashboard for the Domain Monitoring system.

## Install

```bash
cd dashboard
npm install
```

## Run locally

```bash
npm run dev
```

The dashboard expects a running API at `http://localhost:4000/api/monitoring-data`.

## Build

```bash
npm run build
```
